package org.qcri.rheem.ignite.channels;

import org.qcri.rheem.core.optimizer.channels.ChannelConversion;
import org.qcri.rheem.ignite.platform.IgnitePlatform;

import java.util.Arrays;
import java.util.Collection;

/**
 * {@link ChannelConversion}s used by the {@link IgnitePlatform}.
 */
public class ChannelConversions {

    public static Collection<ChannelConversion> ALL = Arrays.asList(

    );
}
